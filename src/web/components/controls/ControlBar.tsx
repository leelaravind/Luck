// OWNER: betting-table agent. STUB — replaced by the real apron control bar (props in ../../contracts.ts).
import type { ControlBarProps } from '../../contracts';

export type { ControlBarProps };

export function ControlBar(_props: ControlBarProps) {
  return <div aria-label="Controls (loading)" />;
}
export default ControlBar;
