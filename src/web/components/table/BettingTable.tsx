// OWNER: betting-table agent. STUB — replaced by the real European layout (props in ../../contracts.ts).
import type { BettingTableProps } from '../../contracts';

export type { BettingTableProps };

export function BettingTable(_props: BettingTableProps) {
  return <div aria-label="Betting table (loading)" />;
}
export default BettingTable;
