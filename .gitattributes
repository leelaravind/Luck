# Keep the original Stitch exports byte-for-byte (no EOL conversion)
design-references/** -text -diff
# Normalise source files to LF in the repository
* text=auto eol=lf
*.ps1 text eol=crlf
*.png binary
